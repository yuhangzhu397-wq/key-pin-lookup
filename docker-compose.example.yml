services:
  key-pin-lookup:
    build: .
    image: joymaas/key-pin-lookup:local
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "127.0.0.1:8787:8080"
    read_only: true
    tmpfs:
      - /tmp:size=16m,mode=1777
    security_opt:
      - no-new-privileges:true
